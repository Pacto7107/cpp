#include <iostream>
#include <algorithm>
#include <string>
#define fr(i,s,e) for(int (i) =(s); (i) < (e); (i)++)
using ll = long long;

using namespace std;

int main() {
    cin.tie(NULL);
    cout.tie(NULL);
    ios::sync_with_stdio(false);
    ////////////

    ////////////
    return 0;
}
